# SB-04 acceptance review

**Reviewed implementation:** `c0778f0` (with review record commit `fc0814e`)  
**Review status:** accepted for merge behind the existing beta build flag; not approved for production deployment  
**Review evidence date:** 2026-09-22 HST

## Findings and resolution

- The independent exact-SHA review found no authorization, slot-binding, transaction-capacity, exact-retry, privacy, App Check-boundary, or alpha-preservation blocker.
- The review found that the callable's 32 KiB request guard counted JavaScript UTF-16 code units instead of UTF-8 bytes. `c0778f0` now uses `Buffer.byteLength(..., 'utf8')`; its regression payload is below the old character limit but above the byte limit and produces no receipt.
- The review found a documentation mismatch for `Admission.roomClosed`. The contract, typed model, backend projection, live client, and regression coverage now agree.
- The four-browser test requires positive authorized RTDB frame captures before asserting forbidden markers are absent, preventing a handshake-only false positive.

## Accepted evidence

- Slot configuration: 1/1 test.
- RTDB rules and alpha preservation: 5/5 tests.
- Callable transactions, races, capacity, closure, and retry: 7/7 tests.
- Firebase adapter/callable: 2/2 tests.
- Typed session model: 11/11 tests.
- Four simultaneous Chromium identities: GM, two players, and presenter; UI and inbound RTDB frame privacy passed.
- Beta typecheck/package and alpha smoke, cases, HTML, and Hawaiʻi setting checks passed.

## Dependency disposition

`npm audit --omit=dev` reports two moderate transitive findings in `gaxios@6.7.1` and `uuid@9.0.1`. They arrive through Google/Firebase packages; Signal Bleed does not call the affected UUID v3/v5/v6 buffer APIs. No compatible, verified non-breaking tree update is currently established. The closed beta temporarily accepts this bounded risk; recheck the upstream tree before the live canary and do not carry the acceptance into SB-13 production cutover without a fresh assessment.

## Merge and release boundary

This artifact plus the project owner's merge authorization satisfies the repository's independent-review gate when a second GitHub account is unavailable. It does not authorize publishing rules, deploying Functions, configuring GM slots, exposing beta routes, or replacing alpha routes. Those actions remain gated by the canary runbook and the protected-file confirmation in `AGENTS.md`.
